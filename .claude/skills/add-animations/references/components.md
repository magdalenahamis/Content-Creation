# Animation Components

Component templates have moved to the `reel-style` skill:

`C:/Content-Creation/.claude/skills/reel-style/references/components.md`

Read that file for copy-paste code for:
- `Caption` — word-by-word reveal, white on video / dark on light bg
- `SplitScreen` — top animation, bottom face-cam
- `TextCard` — bold text on light gray, Mode 3 pure animation
- `SectionLabel` — clean fade label for list-format reels
- `StatAccent` — bold number/stat, no background box
- `theme.ts` — color palette and Inter font setup

Do not use the old components (they had yellow accents, dark overlay boxes,
pill badges, and flash transitions — all removed).
